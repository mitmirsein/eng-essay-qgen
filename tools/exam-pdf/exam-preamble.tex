\usepackage{kotex}
\usepackage{csquotes}
\usepackage{xcolor}
\usepackage{array}
\usepackage{multicol}
\usepackage{fancyhdr}

% ── Fonts ─────────────────────────────────────────────────────────────
% Latin: Noto Serif (pairs with Noto Serif KR). Circled numerals ①..⑳
% resolve through the Hangul-font fallback.
\setmainfont{Noto Serif}[Scale=0.96]
\setsansfont{Noto Sans}
\setmainhangulfont{Noto Serif KR}[InterHangul=-0.02em]
\setsanshangulfont{Noto Sans KR}

% ── Two-column region (multicol): last page balances, the separator rule
%    only spans the actual text height — no rule down an empty column. ──
\setlength{\columnseprule}{0.3pt}
\setlength{\columnsep}{22pt}
\renewcommand{\columnseprulecolor}{\color{black!40}}
\setlength{\multicolsep}{0pt}
\setlength{\premulticols}{0pt}
\setlength{\columnsep}{22pt}

% ── Paragraph metrics: tight, exam-like ──────────────────────────────
\setlength{\parindent}{0pt}
\setlength{\parskip}{1.3pt plus 0.6pt}
\linespread{1.0}
\frenchspacing
\clubpenalty=10000
\widowpenalty=10000

% ── Reading-passage / 보기 box: light shaded, NON-breakable (kept whole
%    so it never fights multicol's column breaking). ──────────────────
\definecolor{passagebg}{gray}{0.945}
\newsavebox{\msexampassage}
\setlength{\fboxsep}{5pt}
\renewenvironment{quote}{%
  \par\nopagebreak\vspace{1pt}%
  \begin{lrbox}{\msexampassage}%
  \begin{minipage}{\dimexpr\linewidth-2\fboxsep\relax}%
    \small\setlength{\parskip}{1.6pt}\setlength{\parindent}{0pt}%
}{%
  \end{minipage}%
  \end{lrbox}%
  \noindent\colorbox{passagebg}{\usebox{\msexampassage}}%
  \par\vspace{2pt}%
}

% ── Custom full-width exam header (printed before the columns start) ──
\makeatletter
\newcommand{\exam@subtitle}{}
\newcommand{\exam@meta}{}
\newcommand{\exam@field}{이름 \underline{\hspace{7em}}}
\newcommand{\setexamsubtitle}[1]{\renewcommand{\exam@subtitle}{#1}}
\newcommand{\setexammeta}[1]{\renewcommand{\exam@meta}{#1}}
\newcommand{\setexamfield}[1]{\renewcommand{\exam@field}{#1}}
\renewcommand{\maketitle}{%
  \thispagestyle{plain}%
  \begin{center}
    {\fontspec{Noto Sans KR}\bfseries\Large \@title\par}
    \vspace{3pt}
    {\fontspec{Noto Sans KR}\large \exam@subtitle\par}
  \end{center}
  \vspace{4pt}
  {\color{black!75}\hrule height 1.1pt}
  \vspace{3.5pt}
  \noindent
  {\fontspec{Noto Sans KR}\normalsize
   \exam@field\hfill \exam@meta\par}
  \vspace{2.5pt}
  {\color{black!75}\hrule height 0.4pt}
  \vspace{9pt}
}
\makeatother

% ── Footer: centered page number ─────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}
\fancyfoot[C]{\small\thepage}
\fancypagestyle{plain}{\fancyhf{}\fancyfoot[C]{\small\thepage}%
  \renewcommand{\headrulewidth}{0pt}\renewcommand{\footrulewidth}{0pt}}
